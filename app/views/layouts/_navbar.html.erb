<nav class="navbar navbar-expand-lg navbar-light bg-light nav justify-content-center">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">Snowboard Marketplace</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="<%= products_path %>"><%= html_escape('Snowboards') %></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_product_path %>"><%= html_escape('Sell my snowboard') %></a>
        </li>
        <% if user_signed_in? %>
        <li class="nav-link">
          <%= link_to "Log Out", destroy_user_session_path, :method => :delete %>
          <%= link_to "Edit Profile", edit_user_registration_path %>
        </li>
        <% else %>
        <li class="nav-link">
          <%= link_to "Log In", new_user_session_path %>
        </li>
        <li class="nav-link">
          <%= link_to "Sign Up", new_user_registration_path %>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>